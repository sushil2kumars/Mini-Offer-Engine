import 'styles/looplink.css';  // you can specify a different stylesheet if needed
import 'styles/example.scss'; // example SCSS import
import 'base/common';  // always import common.js for shared functionality

import Alpine from 'alpinejs';

// you can add additional imports or Alpine plugins here, before Alpine starts

Alpine.start();
